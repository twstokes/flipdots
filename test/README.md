## PlatformIO project used to test FlipDotMatrix

Due to constraints set by dependencies, we compile for and run tests on an ESP8266 that must be connected to the host machine.

To run tests: `pio test`
